# =============================================================================
# IPM Platform - Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values
#
# IMPORTANT: NEVER commit the .env file to git
# Add .env to .gitignore
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# Get these values from your Supabase project dashboard:
# https://app.supabase.com -> Your Project -> Settings -> API

# Your Supabase project URL (e.g., https://xxxx.supabase.co)
VITE_SUPABASE_URL=

# Your Supabase anonymous (public) key
# This is a PUBLIC key, safe for client-side use
# Security is enforced through RLS policies in the database
VITE_SUPABASE_ANON_KEY=

# -----------------------------------------------------------------------------
# SECURITY WARNINGS
# -----------------------------------------------------------------------------
# - NEVER use SERVICE_ROLE_KEY on the client side!
# - SERVICE_ROLE_KEY bypasses RLS and should only be used on secure servers
# - The ANON_KEY is designed to be public and safe for browser use
# - All security is enforced through RLS policies in the database
